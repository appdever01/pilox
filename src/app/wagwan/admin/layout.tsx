export const metadata = {
  title: "Admin Dashboard | PILOX",
  description: "Admin dashboard for PILOX. Manage users, feedback, and settings."
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 