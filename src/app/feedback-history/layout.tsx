export const metadata = {
  title: "Feedback History",
  description: "View your feedback history and rewards.",
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 