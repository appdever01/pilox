export const metadata = {
  title: "Free PDF Page Organizer | Rearrange & Reorder PDF Pages | PILOX",
  description: "Easily reorganize PDF pages with drag-and-drop simplicity. Split, merge, and reorder PDF documents. No installation needed - edit PDFs directly in your browser.",
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 