export const metadata = {
  title: "Pricing Plans & Subscriptions | PDFX",
  description: "Explore PDFX pricing plans and choose the perfect subscription for your document management and AI assistant needs.",
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 