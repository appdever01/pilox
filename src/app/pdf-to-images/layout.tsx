export const metadata = {
  title: "Convert PDF to Images Online | Free High-Quality PDF to JPG Converter | PILOX",
  description: "Convert PDF to JPG, PNG & more with perfect quality. Extract images from PDF files, maintain resolution, and batch convert multiple pages. Free, fast, and secure PDF to image conversion tool.",
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 