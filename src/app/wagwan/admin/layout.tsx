export const metadata = {
  title: "Admin Dashboard | PDFX",
  description: "Admin dashboard for PDFX. Manage users, feedback, and settings."
};

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 